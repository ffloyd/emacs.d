;;
;; User configuration
;;

(defun ffe/user-pre-init ()
  "This function invokes before any of 0xFFE modules loaded."
  nil) ;; Add your code instead of nil

(defun ffe/user-post-init ()
  "This function invokes after all 0xFFE modules loaded."
  nil) ;; Add your code instead of nil
